// Obsoleto: el servidor de desarrollo/produccion ahora lo gestiona Vite
// (`bun run dev` / `bun run build`), no un servidor Bun.serve manual.
// Pendiente de borrado, ver solicitud de eliminacion en el mismo commit.
export {};
