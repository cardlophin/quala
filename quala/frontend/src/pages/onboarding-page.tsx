// Obsoleto: la conexion Databricks ya no es un paso de onboarding a
// pantalla completa. Su formulario vive ahora en
// src/components/shared/connection-form.tsx, usado desde /connections
// (dialog) y desde /projects/new (contextual). Pendiente de borrado.
export {};
