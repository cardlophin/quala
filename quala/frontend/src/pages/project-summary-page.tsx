// Eliminado en el refactor a canvas de grafo (React Flow): /projects/:id
// pasa a ser directamente el canvas (ver project-canvas-page.tsx), no una
// pagina resumen separada. La info de conexion Databricks del proyecto se
// muestra ahora en la cabecera del propio canvas. Se elimina del
// workspace real al sincronizar.
export {};
